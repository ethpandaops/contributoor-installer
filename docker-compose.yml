version: "3.8"

services:
  sentry:
    image: mattevansnz/contributoor-test:latest
    command: ["sentry", "--config", "/config/contributoor.yaml"]
    volumes:
      - ${CONTRIBUTOOR_CONFIG_PATH}:/config:ro
    restart: always
